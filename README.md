# 📊 A股自动分析 & 微信推送

每个交易日早上 6:00 自动复盘上一个交易日，通过方糖推送到微信 —— 睁眼就能看到昨天的盘面结论。

**🔥 零外部依赖** — 全部使用 Python 内置模块，无需安装任何第三方库。

## 功能

- 📈 **技术指标**：MA均线/RSI/MACD/布林带/量价分析
- 🎯 **波段策略**：综合多指标给出买入/观望/回避信号
- 📱 **微信推送**：方糖ServerChan，早 6:00 自动推送到手机
- ⏰ **定时运行**：GitHub Actions，每天 06:00（北京时间）自动执行
- 🔧 **可扩展**：JSON/YAML配置，加减股票只改一行
- 🪶 **零依赖**：仅用Python标准库，GitHub Actions和本地均可直接运行

## 快速开始

### 1. 注册方糖 & 获取 SendKey

1. 访问 [方糖 ServerChan](https://sct.ftqq.com/) → 微信扫码登录
2. 复制你的 **SendKey**（页面顶部就能看到）

> 免费版每天可推送 5 条消息，完全够用

### 2. Fork 本项目

```bash
# GitHub 页面点 Fork，或者：
git clone https://github.com/你的用户名/stock-analyzer.git
cd stock-analyzer
```

### 3. 配置 Secrets

在 GitHub 仓库页面：
**Settings → Secrets and variables → Actions → New repository secret**

| Name | Value |
|---|---|
| `SERVERCHAN_SENDKEY` | 你的方糖 SendKey |

### 4. 配置股票列表

编辑 `config.json`：

```json
{
  "stocks": [
    {"code": "002170", "name": "芭田股份", "market": "sz"},
    {"code": "600519", "name": "贵州茅台", "market": "sh"},
    {"code": "300750", "name": "宁德时代", "market": "sz"}
  ],
  "analysis": {
    "kline_days": 120
  }
}
```

> market: `sh`=上海, `sz`=深圳, `bj`=北京

### 5. 手动测试

在 GitHub 仓库页面：
**Actions → Daily Stock Analysis → Run workflow → Run workflow**

等待1-2分钟，查看微信是否收到推送。

### 6. 完成 🎉

之后每天早上 06:00（北京时间）会自动复盘上一个交易日。也可以在本地运行：

```bash
# 无需安装任何依赖！直接用 Python 内置模块
set SERVERCHAN_SENDKEY=你的SendKey    # Windows
export SERVERCHAN_SENDKEY=你的SendKey  # Linux/Mac

python run_daily.py        # 推荐：分析 + 老龙反抽 + 郑希研报 + 推送
python analyzer.py         # 只跑自选股技术面（推送正文与上面同款）
```

## 推送效果示例

微信卡片里是**纯文本表格**：一票一行，左边 emoji 就是信号档，名称/涨跌/评分竖着对得齐，
手机上不折行。

```
📊 9月18日复盘｜13只·偏多7·空3

▎大盘

上证　　3912　+0.94%

深成　　13641　+1.72%

创业板　3373　+2.25%

▎自选表现

涨12 · 跌1 · 平0 · 均幅 +2.11%

🟢买入0 · 🔹偏多7 · 🟡观望3 · 🔴偏空3

▎今日变化 6只

🔺 光华科技 观望→偏多

🔺 牧原股份 偏空→观望｜站上 MA5/MA10

🔽 翔鹭钨业 偏多→观望｜RSI 44.6 偏弱

▎值得关注

🔹 东方明珠 8.84 +1.96% · +56分

　 差均线未多头排列｜主力+1.72亿

▎自选全览 13只

　 名称　　　涨跌　　评分

🔹 东方明珠　 +1.96%　+56

🔹 光华科技　 +3.98%　+50

🔹 华纬科技　 +2.32%　+50

🟡 牧原股份　 +2.49%　+20

🟡 翔鹭钨业　 -0.90%　 -1

🔴 芭田股份　 +0.92%　-27

🔴 值得买　　 +0.70%　-37

▎老龙反抽 5/22只入选

🚀 阳光电源(300274) 74分

　 逆变器·储能龙头，近20日-22.3%

—— 仅供复盘参考，不构成投资建议
```

> 全角空格撑列宽、每个段落 = 卡片里的一行、为什么行宽卡在 20 个汉字，
> 以及为什么不给「偏多」买点，见下面的「推送格式」一节。

## 项目结构

```
├── .github/workflows/daily-analysis.yml   # GitHub Actions 定时任务
├── config.yaml                            # 股票列表 & 配置
├── run_daily.py                           # 每日运行入口（分析 → 合并 → 推送）
├── analyzer.py                            # 自选股分析主程序
├── data_fetcher.py                        # 行情/K线/资金流抓取（腾讯 + 东财 + 新浪）
├── indicators.py                          # 技术指标计算
├── signals.py                             # 信号词表（档位名/emoji 唯一来源）
├── swing_strategy.py                      # 波段信号评分（趋势/动能/位置/量价/资金）
├── formatter.py                           # 归档报告（Markdown）格式化
├── push_format.py                         # 微信推送正文排版（纯文本表格，手机不折行）
├── yypz_strategy.py                       # yyPZ·老龙反抽策略
├── zhengxi_report.py                      # 郑希视角研报
├── notifier.py                            # 方糖推送
├── scripts/check_push_layout.py           # 排版体检 + 手机预览图
├── scripts/weekly_review.py               # 周复盘
├── scripts/backtest_signals.py            # 信号回测（分布 + 前瞻收益校验）
├── scripts/archive_report.sh              # 日报归档到 reports/daily/YYYY/MM/
└── requirements.txt                       # 依赖
```

## 信号说明

| 信号 | 含义 | 触发条件 |
|---|---|---|
| 🟢 强烈买入 | 趋势、动能、资金共振 | 评分 ≥ 50 **且** 均线多头 **且** RSI<70 **且** 资金不流出 |
| 🔹 偏多 | 多头因子占优 / 高分但确认不足 | 25 ≤ 评分 < 50（或够 50 分但缺上面任一确认） |
| 🟡 观望 | 多空力量接近 | -25 < 评分 < 25 |
| 🔴 偏空 | 空头因子占优 | -50 < 评分 ≤ -25 |
| ⚠️ 回避 | 趋势与动能同步走坏 | 评分 ≤ -50 |

评分由五个维度加权而成：趋势 ±36、动能 ±30、位置 ±25、量价 ±10、资金 ±12。
**超买/超卖按趋势方向解读**——上升趋势里的超买不扣分，下跌趋势里的超卖也不加分（避免接飞刀）。

### 为什么「强烈买入」要卡这么死

本自选池 150 个交易日、1885 个样本、前瞻 5 日的回测（`python scripts/backtest_signals.py`）：

| 口径 | 样本 | 5 日超额 | 胜率 |
|---|---|---|---|
| 评分 ≥50（旧口径） | 217 | +1.01% | 51.2% |
| 评分 ≥50 + 多头排列 + RSI<70 + 资金不流出 | 59 | **+2.80%** | **69.5%** |
| 评分 25~50（「偏多」） | 402 | **-0.77%** | 45.5% |

- 新口径去极值后超额 +2.28%，逐股留一（去掉任意一只票）超额仍全为正，前后半段 +1.30% / +3.01%
- 「偏多」档三段行情全为负，**它不是买点**，只用来做强弱排序，所以推送里和「强烈买入」分开展示
- 评分 ≥50 但 RSI ≥70 的样本 5 日中位数 -1.10%——评分高不等于能买，均线没走多头、已经过热、
  资金在流出，都属于「看着强但没人接」，一律降级观察
- 单笔风险按买点 -6% 封顶，盈亏比不足 1.5 会标注「偏低，仓位减半」
- ⚠️ 样本只有 8~13 只自选票、7 个月，属于小样本，结论会随行情漂移，别当铁律

## 数据来源

- **腾讯财经 API**：实时行情、日K线（前复权）
  - 注意：接口内嵌的 62/70/71 号字段是「年初至今/20日/60日涨跌幅」，**不是资金流向**
- **资金流向**：东方财富 `push2his.eastmoney.com` 的 `fflow/daykline/get`（主力/大单/超大单/中单/小单全口径）
  - ⚠️ 只有 `push2his` 这个域名给历史数据（`push2` / `push2delay` 只返回 1 行），
    路径必须是 `fflow/daykline/get`；写错的话请求会 200 但拿不到数据，静默退化成新浪口径
  - 备用源：新浪资金流（超大单口径），东财不可用时自动切换并在推送中如实标注口径
  - 两个源都失败时自动隐藏资金相关内容，不会输出错误数据
  - 东财按 IP 限频，连续失败会熔断该域名一段时间（正常现象，会自动走备用源）
- 支持沪深京全部交易所

## 推送格式

推送正文由 `push_format.py` 生成。微信服务号卡片不是网页，它有四条硬约束，
现在的排版（**纯文本模拟表格**）就是围绕这四条设计的：

1. **一个段落 = 卡片里的一行**——正文按空行切段，段内写 `\n` 不换行（会被折成一个空格）。
   所以每行都是一个独立段落，段落之间用 `\n\n`（`push_format.PARA`）拼。
2. **ASCII 空格会被折叠**——连续多个只显示一个，行首的还会直接消失。这一条决定了
   列对齐**只能**靠全角空格 `　`（U+3000）：它是普通字符，宽度正好一个汉字，
   既不折叠也不被吃掉。表格的列宽就是靠它撑起来的。
3. **Markdown 不渲染**——`**加粗**`、`#`、表格、`- ` 列表在卡片端会露出原始符号
   （点进详情页才渲染），所以全篇只走纯文本 + emoji。
4. **一行约 20 个汉字**，超了手机就会折行，而折行会把表格打散。所有行都过一遍
   `_fit()` 做宽度截断：宁可截到标点处断句，也不让手机去折行。

宽度不是拍脑袋估的：`_HALF_EM` / `_UPPER_EM` 是拿微软雅黑在 1000px 实测出来的
半角字符宽度表（`M` 是 `I` 的三倍宽，用平均值会把含 `MA5` 的行估窄）。

### 本地预览排版（不发送）

```bash
python scripts/check_push_layout.py --png reports/preview_push.png
```

用内置样例数据渲染出一张 375dp 手机预览图（emoji 是真彩色的），
并逐行给出「估算宽度 / 真实字体实测宽度」，标出哪一行会在手机上折行 ——
改完排版**先看这张图**再提交。也可以体检真实输出：

```bash
python scripts/check_push_layout.py reports/push_2026-09-18.md
```

只生成不发送（不联网跑全流程）：

```bash
PUSH_DRY_RUN=1 python run_daily.py            # Linux/Mac
set PUSH_DRY_RUN=1 && python run_daily.py     # Windows cmd
```

不配 `SERVERCHAN_SENDKEY` 时同样只生成不发送。生成的正文会存到
`reports/push_<数据日期>.md` 并随日报归档 —— 微信里看到的和仓库里存的必须一致。

### 几条内容口径

- 「今日变化」优先于「今日状态」，避免每天推送内容雷同；降级条目只引用 ❌/⚠️ 的理由
  （降级却引一条「均线多头排列」，比不说还糟）
- 「值得关注」里只有「强烈买入」才给买点/止损/目标，偏多档不给可执行价位
- 资金流数据若滞后于行情日期，行内标注〔MM-DD〕；这一行放不下时整条略去，
  绝不截断（数字截一半比不写更误导）
- 顶部日期为**行情数据日期**（早 6 点跑的是昨天，不会标成今天）
- 「自选全览」表格按评分降序，档位由 emoji 列区分，不再另起分组标题
- 郑希观点语料超过 60 天未更新时自动隐藏该板块

## 注意事项

- GitHub Actions cron 有最多 15 分钟延迟
- 法定节假日仍会触发（但数据为上一交易日）
- 东方财富 API 有频率限制，多只股票间隔 1 秒
- 方糖免费版每天 5 条，多只股票合并为 1 条推送
- **技术分析仅供参考，不构成投资建议**

## 扩展

### 添加更多股票

编辑 `config.yaml` 的 `stocks` 列表即可，支持无限添加。

### 调整推送时间

编辑 `.github/workflows/daily-analysis.yml` 的 cron 表达式。cron 用的是 **UTC**，
比北京时间少 8 小时，而且**跨天**——早 6 点属于「前一天晚上 22:00 UTC」：

```
# 当前：每天 06:00 北京时间 = 22:00 UTC（前一天）
# 0-5 覆盖周日~周五 UTC，对应周一~周六北京时间（周六早上那次复盘周五收盘）
- cron: '0 22 * * 0-5'

# 如果还想改回收盘后推送（北京 18:00 = UTC 10:00，不跨天）：
- cron: '0 10 * * 1-5'
```

改时间后**别动** job 里的 `TZ: Asia/Shanghai`：22:00 UTC 触发时 runner 的 UTC 日期
还是「昨天」，不锁时区的话 Python 写的日期和 shell 的 `date` 会差一天，归档会找不到文件。

### 本地定时运行（不用 GitHub Actions）

```bash
# Windows 任务计划程序
# Linux crontab
30 15 * * 1-5 cd /path/to/stock-analyzer && python analyzer.py
```

## License

MIT
